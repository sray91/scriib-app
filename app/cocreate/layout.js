import { Metadata } from 'next'

export const metadata = {
  title: 'Co-Create - Scriib',
  description: 'Collaboratively create and edit content with AI assistance using our interactive canvas.',
}

export default function CoCreateLayout({ children }) {
  return children
} 