import { Metadata } from 'next'

export const metadata = {
  title: 'Content Dashboard - Scriib',
  description: 'Manage and view all your content posts, drafts, and scheduled content in one place.',
}

export default function ContentLayout({ children }) {
  return children
} 