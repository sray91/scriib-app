import { Metadata } from 'next'

export const metadata = {
  title: 'Post Forge - Scriib',
  description: 'Create, organize, and manage your social media posts with our powerful kanban board workflow.',
}

export default function PostForgeLayout({ children }) {
  return children
} 